import React from 'react';
import TrendMasonry from '../growth/TrendMasonry';

const GrowthPage: React.FC = () => {
  return (
    <div className="h-full animate-in fade-in slide-in-from-bottom-4 duration-500">
      <TrendMasonry />
    </div>
  );
};

export default GrowthPage;
