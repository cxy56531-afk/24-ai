# Legacy Files
The files `index.tsx`, `App.tsx`, and `index.html` were moved here from the root directory as part of the migration to Next.js App Router.
The application now uses `app/layout.tsx` and `app/page.tsx` as entry points.